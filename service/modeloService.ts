// Service de exemplo para projeto modelo
export default class ModeloService {
    static async doSomething(param: string): Promise<string> {
        // Lógica de exemplo
        return `Exemplo executado com: ${param}`;
    }
}
